[tox]
minversion = 4.14.1
envlist = py311, mypy
isolated_build = true

[gh-actions]
python =
    3.11: py311, mypy, flake8


[testenv]
setenv =
    PYTHONPATH = {toxinidir}
deps =
    -r{toxinidir}/requirements_dev.txt
commands =
    pytest --basetemp={envtmpdir}



[testenv:mypy]
basepython = python3.6
deps =
    -r{toxinidir}/requirements_dev.txt
commands = mypy chipiron